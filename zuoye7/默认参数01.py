# 默认参数   如果传参则替换默认值     如果不传参则会以默认值输出
def a(name="张三", age=10000000):
    print(f"name  = {name}, age = {age}")


a("李四", 999999999999)   # name  = 李四, age = 999999999999        传参会覆盖默认值
a()                       # name  = 张三, age = 10000000          不传参则使用默认值